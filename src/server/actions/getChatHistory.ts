// Return empty array for now - let the frontend fetch from API
const getChatHistory = async () => {
    // Don't return mock data - let Redux fetch real data from API
    return []
}

export default getChatHistory

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Required for Next.js SSR/API routes on Netlify (needed for NextAuth)
[[plugins]]
  package = "@netlify/plugin-nextjs"
